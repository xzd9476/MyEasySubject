<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.user.dao.LoginInfoDao">
	<insert id="addUserLoginInfo" parameterType="com.user.pojo.LoginInfo">
		insert into login_info(user_id, user_name, status, create_time)
		values(#{userId},#{userName},#{status},#{createTime})
	</insert>
</mapper>
